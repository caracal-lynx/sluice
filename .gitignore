node_modules/
dist/
.env
clients/
output/
*.duckdb
.tmp/
coverage/
*.tsbuildinfo

# Stray developer artefacts (not tracked)
npm-audit.txt
run.log

# Astro/Starlight build cache (docs-site/)
.astro/

# Claude Code — private context never belongs in this public repo.
# Ignore the whole .claude/ tree; a lefthook pre-commit job
# (scripts/check-no-private-context.ps1) backstops any `git add -f`.
.claude/
CLAUDE.local.md
./standards
