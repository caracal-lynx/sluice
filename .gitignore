node_modules/
dist/
.env
clients/
output/
*.duckdb
.tmp/
coverage/
*.tsbuildinfo

# IDE / agent / local-only — Claude Code writes settings, scheduled-task locks,
# and worktree state under .claude/; none of it should be tracked.
.claude/

# Stray developer artefacts (not tracked)
npm-audit.txt
run.log

# Astro/Starlight build cache (docs-site/)
.astro/
